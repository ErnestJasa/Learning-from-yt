// tutorial : https://youtu.be/30LWjhZzg50?t=4539
function addTwo(num) {
    return num + 2;
}
function getUpper(value) {
    return value.toUpperCase();
}
function signUp(name, email, password) { }
var logiUser = function (name, email, paid) { };
function getValue(myVal) {
    if (myVal > 5) {
        return true;
    }
    return "200 OK";
}
var heroes = ["thro", "spiderman", "batman", 2];
heroes.map(function (hero) {
    return 2;
});
addTwo(1);
